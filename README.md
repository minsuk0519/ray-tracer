# ray-tracer

Monte Carlo path tracer (C++). Build with `make`, run `./raytrace.exe <scene.scn>`; output is `<scenename>.hdr`.

Requires [Assimp](https://github.com/assimp/assimp) (e.g. `brew install assimp` on macOS; Makefile expects `/opt/homebrew/opt/assimp`).
